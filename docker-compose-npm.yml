version: '3.4'

# Services definition for running npm commands

services:

  npm:
    # Build
    image: ${IMAGES_PREFIX:-app}-npm:${DOCKER_TAG:-latest}
    build:
      context: ./streamlit_dsfr
      dockerfile: ./Dockerfile
      target: npm
    # Deploy
    volumes:
      - .streamlit_dsfr/frontend:/app
